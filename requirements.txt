# Module 11 — Integration: Compose Evaluation
# M10 backend dependencies + M11 instrumentation + evaluation deps.
fastapi==0.110.0
uvicorn==0.27.0
pydantic==2.6.0
httpx==0.27.0
prometheus-client==0.20.0
pytest==8.0.0
# pytest-asyncio is not needed -- the harnesses and autograder are sync
# (TestClient + httpx). Was previously pinned at 0.25.0 which requires
# pytest>=8.2 and made the resolver fail.
